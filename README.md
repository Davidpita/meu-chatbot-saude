# meu-chatbot-saude

Estrutura inicial criada: `backend/` e `frontend/` com arquivos básicos.

- Backend: `server.js`, `package.json`, `.env` (porta 3000)
- Frontend: Vite + React em `frontend/` (`public/index.html`, `src/`, `package.json`, `vite.config.js`)

Para iniciar:
- Backend: cd backend; npm install; npm run start
- Frontend: cd frontend; npm install; npm run dev
